const ObjectDetails = ({ key }) => {
    console.log(key);

    return (
        <div id="obj-details">
            <h1>These are the object details</h1>
        </div>
    );
};

export default ObjectDetails;